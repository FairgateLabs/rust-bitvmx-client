elf: cardinal-verifier.elf
nary_search: 8
max_steps: 500000000
input_section_name: .input
inputs:
  # 4 bytes for the 01000000 (true result)
  # 32 bytes for the lock hash
  - size: 36
    owner: const
  # cert hash set in the slot protocol
  - size: 32
    owner: prover_prev
  # 4 bytes for the group id from the slot protocol
  - size: 4
    owner: prover_prev
  # 128 bytes for the groth16 seal
  - size: 128
    owner: prover